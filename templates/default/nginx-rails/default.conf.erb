upstream <%=node["nginx"]["upstream"]%> {
    server <%=node["rails"]["server"]["socket"]%>;
}

server {
    listen <%=node["nginx"]["port"]%> default_server;
    server_name _;
    location / {
        proxy_pass http://<%=node["nginx"]["upstream"]%>;
    }
}
